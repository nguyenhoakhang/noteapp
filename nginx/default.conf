# server {
#     listen 80;
#     server_name localhost;

#     # Proxy API requests to Laravel backend (artisan serve on port 8000)
#     location /api {
#         proxy_pass http://noteapp_backend:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Proxy all other requests to frontend dev server
#     location / {
#         proxy_pass http://noteapp_frontend:5173;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }
server {
    listen 80;
    server_name localhost;

    # Proxy API requests to Laravel artisan serve
    location /api {
        proxy_pass http://noteapp_backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy all other requests to frontend dev server
    location / {
        proxy_pass http://noteapp_frontend:5173;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}